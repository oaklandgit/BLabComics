<article>
    <h2>
    "{{ item.title}}"
    </h2>
    {% for image in item.images %}
    <div class="imgContainer">
        <img src="{{ url_for('static', filename='full2x/' ~ image) }}">
    </div>
    {% endfor %}
</article>