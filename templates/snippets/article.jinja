<article>
    <h2>
    <a href="/comics/series/{{ item.series }}">
    {{ groupLabels[item.series] }}:
    </a>
    "{{ item.title}}"
    </h2>
    {% for image in item.images %}
        <img src="{{ url_for('static', filename='full2x/' ~ image) }}">
    {% endfor %}
</article>